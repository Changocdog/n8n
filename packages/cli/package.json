{
  "name": "n8n",
  "version": "1.103.0",
  "description": "n8n CLI",
  "main": "index.js",
  "scripts": {
    "start": "run-script-os",
    "start:default": "cd bin && ./n8n",
    "start:windows": "cd bin && n8n",
    "worker": "n8n worker",
    "webhook": "n8n webhook",
    "build": "tsc -p tsconfig.build.json",
    "dev": "nodemon --watch ../../packages --exec \"n8n\""
  },
  "dependencies": {
    "semver": "^7.5.4"
  },
  "devDependencies": {
    "@types/node": "*",
    "ts-node": "^10.9.1",
    "typescript": "*"
  },
  "engines": {
    "node": ">=22.16.0"
  }
}
